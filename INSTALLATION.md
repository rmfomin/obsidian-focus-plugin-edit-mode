# Установка и использование плагина Focus and Highlight

## 🚀 Установка для разработки/тестирования

### Вариант 1: Ручная установка в тестовое хранилище

1. **Найдите папку плагинов в вашем Obsidian vault:**
   ```
   <ваш-vault>/.obsidian/plugins/
   ```

2. **Создайте папку для плагина:**
   ```bash
   mkdir -p <ваш-vault>/.obsidian/plugins/obsidian-focus-plugin
   ```

3. **Скопируйте файлы плагина:**
   Скопируйте следующие файлы из папки проекта в созданную папку плагина:
   - `main.js`
   - `manifest.json`
   - `styles.css`

   ```bash
   # Из папки проекта
   cp main.js manifest.json styles.css <ваш-vault>/.obsidian/plugins/obsidian-focus-plugin/
   ```

4. **Перезапустите Obsidian или обновите список плагинов:**
   - Перезапустите Obsidian полностью, или
   - Откройте Settings → Community plugins → нажмите кнопку обновления

5. **Включите плагин:**
   - Откройте Settings → Community plugins
   - Найдите "Focus and Highlight" в списке
   - Включите переключатель рядом с плагином

### Вариант 2: Использование симлинка (для разработки)

Это удобнее, если вы планируете продолжать разработку:

```bash
# Создайте симлинк из папки проекта в папку плагинов
ln -s /Users/romanfomin/rmfomin-projects/obsidian-focus-plugin-edit-mode <ваш-vault>/.obsidian/plugins/obsidian-focus-plugin

# Теперь при каждой сборке (npm run build) изменения будут сразу доступны
```

После этого просто перезагружайте плагин через:
- Ctrl/Cmd + P → "Reload app without saving"
- Или через Settings → Community plugins → Toggle off/on

## 📖 Использование

### В режиме чтения (Reading/Preview mode):

1. Откройте любую заметку
2. Переключитесь в режим чтения (кнопка книжки или Ctrl/Cmd + E)
3. Кликните на любой заголовок или параграф
4. Все остальное затемнится, оставив выделенным только выбранный блок

### В режиме редактирования (Edit/Source mode):

1. Откройте любую заметку
2. Убедитесь, что вы в режиме редактирования (Source mode)
3. Кликните на строку с заголовком или на любой параграф
4. Все остальные строки затемнятся

### Команды (Ctrl/Cmd + P):

- **"Clear Focus"** - очистить текущий фокус
- **"Toggle Focus Mode"** - включить/выключить режим фокусировки

### Настройки (Settings → Focus and Highlight):

1. **Enabled Focus Mode** - включить/выключить плагин
2. **Clear Method** - как снимать фокус:
   - Click again - кликнуть еще раз на выделенный блок
   - Click outside - кликнуть за пределами контента
3. **Focus Scope** - что фокусировать:
   - Only one block - только сам блок
   - Also the content - блок и всё его содержимое
4. **Content Behavior** (для preview mode) - поведение при клике на контент:
   - Only focus on the element - только элемент
   - Focus related contents - элемент и связанный контент
   - Do nothing - ничего не делать
5. **Enable List** - фокусировка на элементах списка (экспериментально)
6. **Enable Status Indicator** - показывать индикатор в статус-баре
7. **Focus Sensitivity** - чувствительность (задержка в мс)

### Дополнительные настройки стилей:

Если у вас установлен плагин **Style Settings**:

1. Установите [Style Settings plugin](https://github.com/mgmeyers/obsidian-style-settings)
2. Откройте Settings → Style Settings → Focus and Highlight
3. Настройте:
   - **Dim Opacity** - прозрачность затемненных элементов (0-1)
   - **Dim Speed** - скорость анимации (секунды)

## 🎯 Примеры использования

### Пример 1: Фокус на заголовке
```markdown
# Заголовок 1
Содержимое заголовка 1

## Подзаголовок
Содержимое подзаголовка

# Заголовок 2  ← Кликните сюда
Это содержимое будет видно, остальное затемнится
```

### Пример 2: Фокус на параграфе (с настройкой "Only one block")
```markdown
Первый параграф текста.

Второй параграф текста.  ← Кликните сюда (будет виден только он)

Третий параграф текста.
```

## 🔧 Режим разработки

Для разработки удобно использовать:

```bash
# Запустить watch mode (автоматическая пересборка)
npm run dev

# После изменений перезагрузить Obsidian:
# Ctrl/Cmd + P → "Reload app without saving"
```

## ❗ Возможные проблемы

### Плагин не появляется в списке
- Проверьте, что файлы скопированы в правильную папку
- Проверьте, что файл `manifest.json` корректный
- Перезапустите Obsidian полностью

### Плагин не работает
- Убедитесь, что плагин включен (Settings → Community plugins)
- Проверьте консоль разработчика (Ctrl/Cmd + Shift + I) на наличие ошибок
- Убедитесь, что используете Obsidian версии 0.15.0 или выше

### Фокус не работает в edit mode
- Проверьте, что вы действительно в Source mode, а не Live Preview
- Попробуйте кликнуть на строку с заголовком
- Проверьте настройку "Enabled Focus Mode"

### Анимация не работает
- Проверьте, что в Obsidian не отключены анимации
- Проверьте настройки CSS переменных через Style Settings

## 📝 Тестирование

Создайте тестовую заметку с таким содержимым:

```markdown
# Тестовый заголовок 1

Это содержимое первого заголовка.
Несколько строк текста для проверки.

## Подзаголовок 1.1

Содержимое подзаголовка.

# Тестовый заголовок 2

Это содержимое второго заголовка.

Отдельный параграф после пустой строки.

# Тестовый заголовок 3

Еще один заголовок для тестирования.
```

Теперь:
1. Переключайтесь между Reading и Edit mode (Ctrl/Cmd + E)
2. Кликайте на разные заголовки и параграфы
3. Проверьте настройки Focus Scope
4. Попробуйте команду "Clear Focus"
